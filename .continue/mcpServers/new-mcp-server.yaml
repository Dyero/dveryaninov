name: MCP server
version: 0.0.1
schema: v1
mcpServers:
  - name: MCP server
    command: npx
    args:
      - -y
      - "figma-developer-mcp"
      - "--figma-api-key=YOUR_FIGMA_API_KEY"
      - "--stdio"
    env: {}
